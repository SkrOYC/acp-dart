# ACP v1.5 wire fixtures

Source of truth: `agentclientprotocol/typescript-sdk` tag `v1.5.0`.
Pinned checkout: `/tmp/acp-typescript-sdk-v1.5.0`.
Schema: `schema/schema.json`; behavior references: `src/acp.test.ts`,
`src/stream.test.ts`, and `src/schema-deserialize.test.ts`.

Files contain stable ACP v1 message objects unless their filename starts with
`experimental_`. They are JSON wire fixtures, not Dart model snapshots.
